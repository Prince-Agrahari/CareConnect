# CareConnect frontend
# Copy this file to frontend/.env for local development.
# Vite only exposes variables prefixed with VITE_

# Local `npm run dev` (also the fallback in src/api/client.js)
VITE_API_URL=http://localhost:8000

# Production builds use frontend/.env.production instead of this value.
